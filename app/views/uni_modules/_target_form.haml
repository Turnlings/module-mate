= form_for [@uni_module_target] do |f|
  = f.hidden_field :uni_module_id, value: @uni_module.id

  .field
    = f.label :score do
      Target
      %span.font-secondary= " (%)"
    = f.text_field :score

  .actions
    = f.submit 'Save'